using System.Collections.Generic;
using UnityEngine.UI;

namespace CollageMode
{
	public static class UISelectionListHelper
	{
		public static void UpdateVerticalScrollRectToSelected<T>(ScrollRect scrollRect, List<T> entriesList, T selectedEntry, float gamepadScrollSpeed)
		{
		}

		public static void UpdateHorizontalScrollRectToSelected<T>(ScrollRect scrollRect, List<T> entriesList, T selectedEntry, float gamepadScrollSpeed)
		{
		}
	}
}
