using PixelCrushers.DialogueSystem;

namespace statistics
{
	public static class ItemStatisticsCollector
	{
		private const string ITEM_FILE_NAME_PREFIX = "Items";

		private const string THOUGHT_FILE_NAME_PREFIX = "Thoughts";

		private const string THOUGHT_FIELD = "isThought";

		private const string INTERACTABLE_DIALOGUE_PREFIX = "INTERACTABLE";

		public static void CollectStatistics(DialogueDatabase ntwtfDD)
		{
		}

		private static void GetItemList(DialogueDatabase ntwtfDD, Items items, Items thoughts)
		{
		}

		private static void FindDialogueReferences(DialogueDatabase ntwtfDD, Items items)
		{
		}

		private static void LocateContainers(DialogueDatabase ntwtfDD, Items items)
		{
		}
	}
}
